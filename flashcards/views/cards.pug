extend layout.pug

block content
    section#content
      h2= text
      if hint
        p
          i Hint: #{hint}
      a(href=`${id}?side=${side}`) #{sideText}  
      br
      a(href='/cards') Next Card